---
import { getLanguages } from "@/lib/loaders";
import Section from "../section.astro";

const { data: languages } = await getLanguages();
---

<Section
  label="Languages"
  class="grid gap-x-12 lg:grid-cols-[auto_minmax(0,1fr)] [&>h2]:col-span-full"
>
  {
    languages.map((language) => (
      <article class="col-span-full mt-3 grid grid-cols-subgrid items-center lg:mt-2">
        <h3 class="font-bold uppercase">{language.language}</h3>
        <span class="text-sm">{language.level}</span>
      </article>
    ))
  }
</Section>
