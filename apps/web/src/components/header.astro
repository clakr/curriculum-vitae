---
import { getInfos } from "@/lib/loaders";

const { data: infos } = await getInfos();
---

<header>
  <h1 class="text-4xl font-bold">{infos.full_name}</h1>
  <h2 class="text-2xl font-semibold">{infos.position}</h2>
</header>
